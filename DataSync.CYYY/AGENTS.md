# 项目约束

## 数据库变更

- 任何新增、修改、删除数据库结构的代码改动，必须同时新增对应 SQL 迁移文件。
- SQL 迁移文件按月份建目录，格式：`Migrations\yyyy-MM\yyyy-MM-dd_变更说明.sql`。
- SQL 必须可重复执行，优先使用 `IF NOT EXISTS`、`IF EXISTS` 或等价保护。
- 启动时自动补字段的 SQL，也必须同步落到当日迁移文件中。
- 未涉及数据库结构变更时，不新增迁移文件。
